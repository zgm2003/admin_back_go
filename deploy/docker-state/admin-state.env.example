# Process environment defaults used by deploy/docker-state/docker-compose.yml.
# Local state ports bind to loopback only and intentionally avoid FlyEnv defaults.
ADMIN_MYSQL_HOST_PORT=33306
ADMIN_REDIS_HOST_PORT=36379
